# Flask Configuration
FLASK_ENV=development
SECRET_KEY=your-secret-key-change-in-production

# Database Configuration
# SQLite is used by default, but you can configure other databases
# SQLALCHEMY_DATABASE_URI=sqlite:///instance/school_erp.db

# ERP Integration
ERP_WEBHOOK_SECRET=erp-webhook-secret-change-me

# Server Configuration (for production)
PORT=5000

# Optional: JWT Secret (if using JWT tokens)
JWT_SECRET_KEY=your-jwt-secret-key
